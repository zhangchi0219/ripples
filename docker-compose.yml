services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./frontend/dist:/app/frontend/dist:ro

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    environment:
      - API_URL=http://backend:8000
